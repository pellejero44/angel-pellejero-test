import { LazyLoadImagesDirective } from './lazy-load-image.directive';
import { Renderer2, ElementRef, NgZone } from '@angular/core';

describe('LazyLoadImagesDirective', () => {
//   it('should create an instance', () => {
//     const directive = new LazyLoadImagesDirective( ElementRef,Renderer2, NgZone, @Inject(PLATFORM_ID), this.rootElement
//     );
//     expect(directive).toBeTruthy();
//   });
});
